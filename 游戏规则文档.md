# 远征（六边形行军路径规划器）游戏规则文档

> 编写说明：本文档根据当前 `S25` 分支下 `hex_pathfinding_demo.py`（单文件桌面应用，约 7700 行）及其运行时依赖的 `landInfo.json` / `map_S25.csv` 逐段核对源码后整理而成，用于说明"远征"这个策略游戏本身的**规则**（而非软件操作手册）。
>
> **关于 `hex_core.py`**：任务要求重点阅读 `hex_pathfinding_demo.py` 和 `hex_core.py`，但在当前仓库（`S25` 分支）中检索（含 `git log`、`Marching.zip`、`build/`、`dist/`、`__pycache__`）均未找到名为 `hex_core.py` 的文件——它可能存在于其它分支/历史版本、或是记忆有误的文件名。核心的六边形坐标系、寻路与地块逻辑目前**全部实现在 `hex_pathfinding_demo.py` 内**（模块级函数 `_center`/`_neighbors`/`_to_axial`/`_hex_dist`/`_astar` 等），本文档即以此为准。
>
> 仓库里另有 `设计文档.md`，写于更早的 `S24` 赛季版本，文件命名（`map_S24.csv` 等）、Tent 退化天数节点等**部分数值已经过时**；本文档以当前 `S25` 代码与数据为准，两者结构类似但数字以本文档为准。

---

## 1. 游戏是什么

"远征"是一款**策略行军游戏**：玩家指挥最多 **3 支队伍**，在一张固定的六边形地图上，用 **90+ 天**（当前 S25 赛季为 **97 天**，见 `landInfo.json` 的 `season.duration_days`）的时间，逐步占领地块，用有限的"粮食"和"步数"资源换取"积分"，同时触发地图上的各种特殊机制（营地、卦、boss、增益地、传送门、飞雷神）。

`hex_pathfinding_demo.py` 是这个游戏的**电子推演/规划工具**：它不是游戏本身的服务端，而是一个桌面小程序，帮玩家在脱离真实游戏客户端的情况下，先把 90 多天的每日操作在电脑上模拟规划好（点哪个格子、走哪条路），核算清楚粮食/步数/积分是否够用，最后导出成 Excel 操作表，照抄到真实游戏里执行。因此代码里实现的资源公式、地块数值、触发条件，即是对"远征"这款游戏真实规则的还原。

---

## 2. 地图与坐标系统

- 地图是固定的 **60 行 × 60 列**（`ROWS = COLS = 60`，由 `map_S25.csv` 决定）六边形网格，每个格子是一个 **flat-top（平顶朝向）** 正六边形。
- CSV 文件里每个格子是一个 token（字符串），代表该格子的地形类型；程序读入后会整体**上下翻转一次**，使得"内部坐标第 0 行 = CSV 最后一行 = 游戏里的第 1 行"。
- 六边形邻居关系按**列的奇偶性**分两组方向向量（`_neighbors()`），即标准的 "odd-column offset" 六边形网格邻接规则——每个格子恰好有 **6 个相邻格**（边界格少于 6 个）。
- 距离/寻路使用标准六边形轴向坐标转换（`_to_axial`）+ 六边形版曼哈顿距离公式，作为 A* 的可采纳启发函数。
- 地图上有一个唯一的出生点格子（token `ST`），队伍 1 默认从这里出发。
- 数值 `0`（empty）代表**不可通行**的空地，其余所有 token 都是可以走的地形。

---

## 3. 地块类型与基础数值

游戏里的每种地块由三个核心数值定义：
- **粮食（food）**：进入该地块要花费的粮食，**负数表示倒贴给队伍**（相当于该地块给你送粮食）。
- **步数（step）**：进入该地块要占用的步数，**负数表示倒还步数**给队伍。
- **积分（award）**：占领该地块获得的分数。

当前 `landInfo.json`（S25）里定义的全部地块类型：

| Token | 名称 | 粮食 | 步数 | 积分 | 说明 |
|---|---|---:|---:|---:|---|
| `0` | empty（空地） | — | — | — | 不可通行 |
| `1` | land_1 | 100 | 1 | 35 | 普通地块，数字越大成本/积分越高 |
| `2` | land_2 | 110 | 1 | 40 | |
| `3` | land_3 | 120 | 1 | 45 | |
| `4` | land_4 | 130 | 1 | 50 | |
| `5` | land_5 | 140 | 1 | 55 | |
| `6` | land_6 | 150 | 1 | 60 | |
| `M` | Merchant（商人） | 0 | 1 | 30 | 免粮食但占 1 步 |
| `T` | Tent（营地） | **-300**（随天数"退化"，见 §5） | **-1**（倒还 1 步） | 0 | 唯一随天数变差的地块，全地图共 N 个，全部占领是解锁"圈地模式"的条件之一 |
| `g` | smallGua（小卦） | 120 | 1 | 45 | 与 `G` 共同构成 G/g 地 |
| `G` | bigGua（大卦） | 200 | 1 | 300 | 与 `g` 共同构成 G/g 地，全部占领触发全局折扣（见 §6） |
| `b` | midBoss（中boss） | 200 | 1 | 500 | |
| `B` | bigBoss（大boss） | 200 | 1 | 1000 | **首次**占领时，全队共享的飞雷神次数 +1（见 §7.4） |
| `L` | liLian（历练） | 0 | 0 | 520 | 零成本零步数的纯奖励地块（"零步数地块"，占领方式见 §4.2 分类 2） |
| `X1`/`X2`/`X3` | xunji（迅） | 100 | 1 | 35/40/45 | 占领后触发"接下来 N 次移动免步数"增益 |
| `B1`/`B2`/`B3` | bingliang（粮） | 100 | 1 | 35/40/45 | 占领后触发"接下来 N 次移动挑战成本打折"增益 |
| `Z1`/`Z2`/`Z3` | zhanxun（勋） | 100 | 1 | 35/40/45 | 占领后触发"接下来 N 次移动奖励加成"增益 |
| `T2`~`T6` | tower（塔） | 100 | 1 | 40/45/50/55/60 | 普通付费地块的另一种皮肤 |
| `P1`~`P8` | port（传送门） | 0 | 0 | 0 | 每种编号恰好一对，见 §8 |
| `ST` | start（出生点） | 0 | 0 | 0 | |

其中 `X1~3`/`B1~3`/`Z1~3` 三组增益地块，编号 1/2/3 对应的**持续次数固定为 5 / 8 / 10 次**（`effective_steps` 字段，见 §7）。

---

## 4. 队伍与移动的基本规则

### 4.1 队伍
- 最多 3 支队伍（team1/2/3）。team1 从出生点 `ST` 自动创建；team2/team3 需要玩家手动指定起点才会出现，起点通常是当前已有队伍所在的位置。
- **粮食是三支队伍共用的一个全局资源池**；**步数是每支队伍各自独立结算的资源**。这是理解"为什么某支队伍卡住走不动、而粮食其实还有"的关键前提。
- 每个格子一旦被**任意**一支队伍占领过，就永久标记为"已占领"（`all_visited_hexes`），此后任何队伍（包括原队伍自己）再次踩上去都只按"重复经过"计费，不会重复获得该地块的积分。

### 4.2 单次移动的成本分类
队伍每移动到一个新格子，程序按以下优先级对这个格子分类计费（`_process_path_click`）：

1. **重复经过（跳步/jump）**：如果目标格已经被任意队伍占领过，或者本次移动路径里已经路过一次 → 固定花费 **10 粮食**（受 G/g 全局折扣影响，见 §6），**不消耗步数、不获得积分**。
2. **零步数地块直接占领**：目标格的 `step ≤ 0`（当前只有 `T` 营地和 `L` 历练两种）→ 花费"移动成本 50（受 G 折扣）+ 该地块的挑战成本（受 B 折扣/G 折扣影响，Tent 除外）"，获得对应积分，并按地块的 `step` 值增减步数（`T` 会倒还 1 步）。
3. **免费探索 / "蹭步"**：见 §4.3。
4. **正常新占领**：花费"移动成本 50（受 G 折扣）+ 挑战成本（受 B 折扣/G 折扣）"，获得地块积分（受 Z 加成影响）与地块步数。

**折扣叠加规则**（关键公式）：
- B 增益折扣（默认 40%）与 G/g 全局折扣（20%）是**相加**而不是相乘：两者都生效时，挑战成本按 `1 - 0.4 - 0.2 = 0.4` 倍收取（即打 4 折，不是 `0.6 × 0.8 = 0.48`）。
- G/g 折扣**不作用于 Tent 营地**的挑战成本。
- 折扣只作用于"正数成本"，地块的"负成本"（倒贴粮食，如 Tent 的 -300）不受任何折扣影响，原样倒贴。
- "移动成本 50"和"重复经过 10"、"免费探索 50"这三类固定费用，只受 G/g 全局折扣影响，不受 B 折扣影响。

### 4.3 免费探索（"蹭步"）机制
一支队伍如果**当天步数已经用完**，仍然可以"蹭"一步去看相邻的一个未占领格子：

- 触发条件：队伍当日步数余额为 0，本次只点击了 1 个相邻的、`step > 0` 且未被任何人占领过的格子，且当前所在格不是"已探索未结算"的状态。
- 效果：花费固定 **50 粮食**（受 G 折扣影响）"探一下"这个格子，**不消耗步数、不获得积分、不算正式占领**。
- 之后有两种方式正式"买单"占领这个探索格：
  1. **离开**它去别的格子——离开那一刻才正式补交这个格子的挑战成本（受折扣）、计 1 步、拿积分；
  2. **原地点击自己所在的这个探索格**——弹出"是否占领当前试探地块"的确认，确认后同样补交成本、计 1 步、拿积分。
- 如果一直不做以上任一动作，这个探索格的正式成本不会被重复收取，可以无限期"白嫖视野"地停留在上面。

### 4.4 移动距离限制
- 单次点击移动使用 **A\*** 算法自动寻路到目标格（代价函数取该格粮食值，至少为 1；启发函数是六边形距离），移动路径**不能绕开任何格子**（当前版本没有可配置的"墙"）。
- **单次拖拽路径最长 18 格**（含终点、不含起点），超出会被拒绝。

---

## 5. 营地（Tent）的时间退化机制

营地 `T` 的粮食倒贴额度会随天数推进逐步"变差"（越晚占领、倒贴越少）。S25 赛季当前配置（`landInfo.json` 的 `T.degrade` 字段）：

| 天数区间 | 每占领 1 个 Tent 倒贴的粮食 |
|---|---:|
| Day 1 ~ 14 | **-300** |
| Day 15 ~ 24 | **-250** |
| Day 25 ~ 42 | **-200** |
| Day 43 ~ 63 | **-150** |
| Day 64 起 | **-100** |

规则完全数据驱动：代码只是"取 `day ≤ 当前天` 里 `day` 最大的那一档"，具体天数分界和金额都在 `landInfo.json` 里配置，理论上可以给其它地形也加上同样的退化表。

---

## 6. G/g 全局减免（"卦"地里程碑）

- 地图上所有 `G`（大卦，300 分）和 `g`（小卦，45 分）格子构成一个集合。
- 一旦**这个集合里所有格子都被占领过**（不区分哪支队伍占领），立即触发一个**全局、永久生效**的效果：此后所有"挑战成本"（Tent 除外）、"移动成本 50"、"重复经过成本 10"、"免费探索成本 50" **统一打 8 折（省 20%）**。
- 这不是消耗次数的增益，而是一次性达成后永久生效的里程碑，与哪支队伍触发无关。
- 与 B 增益折扣叠加时是相加（见 §4.2）。

---

## 7. 三种可叠加的增益地块（X / B / Z）

地图上有编号 1/2/3 的三组共 9 个增益地块，**新占领**（不是重复经过或探索）到其中任意一个时触发对应增益：

| 增益 | 触发地块 | 效果 | 持续次数（按编号） |
|---|---|---|---:|
| **X（迅）** | X1 / X2 / X3 | 接下来 N 次移动的**步数成本清零**（对重复经过/探索无效） | 5 / 8 / 10 |
| **B（粮）** | B1 / B2 / B3 | 接下来 N 次移动的**挑战成本打 6 折**（可与 G 折扣相加叠加） | 5 / 8 / 10 |
| **Z（勋）** | Z1 / Z2 / Z3 | 接下来 N 次移动的**积分加 40%**（四舍五入） | 5 / 8 / 10 |

- 三种增益**互不冲突、可同时持有**，各自独立倒计时消耗。
- 每支队伍的增益剩余次数各自独立（增益属于队伍，不是全局共享）。
- 消耗规则：每"新占领"1 个格子消耗 1 次；重复经过、免费探索不消耗；一次移动跨越多个新格子时，会按格子逐个消耗直到次数耗尽（超出部分的格子不再享受该增益）；经过传送门（无论选择传送与否）固定算 1 次移动，同样消耗 B/Z 剩余次数。
- 撤销（Undo）一步操作时，当前激活队伍的 X/B/Z 三种增益剩余次数会被**清零**而不是恢复——这是软件的已知简化行为（并非游戏本身规则）。

---

## 8. 传送门（Portal，P1~P8）

- 每个编号的传送门在地图上**恰好成对出现**。
- 当队伍的移动路径**终点**恰好落在某个传送门格上时（无论是正常寻路移动还是飞雷神直接落地），系统会询问"是否传送到对面配对的传送门"。
  - 选择"是"：终点直接改写为配对传送门所在格，两侧传送门都标记为已占领；这条"跨地图"的连线在地图上不会画出来。
  - 选择"否"：停留在当前传送门格，只把当前这一侧标记为已占领。
- **无论选择传送与否，都算作一次移动**，会消耗队伍当前生效中的 B 折扣 / Z 加成各 1 次剩余次数（飞雷神落地直接落在传送门上除外，不计跳步）。
- 若传送到了此前未被任何人占领过的目的地格，该目的地格若恰好是 B1~3/Z1~3 增益地块，同样会正常触发对应增益。

统计口径（用于游戏内"跳步"统计）：
- 普通重复经过（走到已占领格）记 **1 跳**。
- **首次**经过某传送门（无论选是否传送）记 **5 跳**。
- 经过一个**已经被使用过**的传送门记 **1 跳**。
- **飞雷神直接飞落**到传送门格上记 **0 跳**。

---

## 9. 飞雷神（Fly Skill）

- 全队共享一个"飞雷神次数"计数器（`fly_skill_limit`），赛季开局为 **1**；任意队伍**首次**占领大boss（`B`）时，该计数器 **+1**（不限队伍，全局共享增加）。
- 使用飞雷神时，队伍可以**不经过 A\* 寻路、不要求与当前所在格相邻或连通**地直接传送到目标格，但目标格必须同时满足：
  1. 该格**尚未被任何人占领**；
  2. 该格必须**恰好紧邻**（1 跳距离）"当前队伍所在位置所连通的、已占领地块连通区域"的**某一个**边缘格——即只能飞到自己（连通）领地的外沿一圈，不能飞到地图上任意远的地方，也不能飞进已经被占的格子。
     > 说明：软件内提示文案写的是"目标须在领地外沿的两跳范围内"，但代码实际判定是**恰好 1 跳**（`destination in _neighbors(occupied_hex)`），文案与实现存在这一处出入，游戏规则以代码判定（1 跳）为准。
- 消耗：
  - 飞雷神次数 -1；如果落地格是**新占领的大boss**，该次消耗被立即 +1 抵消（等于本次实际不消耗）。
  - 粮食：**免除"移动成本 50"**这一项（大boss除外，大boss落地仍需付 50 移动成本），挑战成本（受 B/G 折扣）照常收取；若落地格已被占领过，则按"重复经过"收取固定 10 粮食。
  - **不消耗队伍步数**，除非落地格的 `step` 属性为负（如 Tent，会倒还步数）。
- 若目标格是"可占领、非传送门、非营地"这类正常地块，飞雷神落地时**并不立即结算**（不扣挑战成本、不加分），而是记为"探索"状态，等队伍之后离开该格或原地确认占领时才正式结算——与 §4.3 免费探索的"先看后买单"逻辑一致。
- 落地后同样会触发传送门确认、B/X/Z 增益判定。
- 再次点击飞雷神按钮可取消本次飞行选择模式。

---

## 10. 天数与资源结转系统

### 10.1 粮食（三队共享）
- Day 1 起始粮食 **6800**；**从 Day 2 起，每天固定 +1600**（公式：`当日可用粮食 = 6800 + 1600 × (day - 1)`）。
- 粮食是跨天累计结转的：某天没花完的粮食会带到下一天继续用。
- 粮食不足以支付某次移动的花费时，该次移动会被直接拒绝。

### 10.2 步数（每队独立）
- 每支队伍**每天固定恢复 +6 步**，但**累计步数上限为 18**（即最多允许连续攒 3 天不动，攒到 18 步封顶，不会再多）。
- 每次移动同时需要检查"粮食是否够"和"当天步数是否够"，任一条件不满足都会拒绝这次移动，提示应等到下一天。
- 一次点击移动产生的资源消耗，**永远记在玩家当前正在查看的那一天**上，而不是该队伍历史上最后一次实际行动的天数——这允许"某天故意只走一半步数、留着攒到后面某天再一次性用掉"的打法。唯一的保护限制是：不允许把新动作记到"早于该队伍历史最后行动天"的天数上（防止误改写过去）。

### 10.3 每周固定积分加成
- **Day 1**，以及此后每逢 `(day - 1) % 7 == 3` 的天（即第 4、11、18、25... 天），**当天自动获得 +500 积分**，与队伍是否行动无关（类似"周常任务奖励"）。

---

## 11. 圈地模式（后期自由规划）

这是一个仅在"清完图"后解锁的高级规划模式，用于把地图上剩余的所有普通地块（此时资源已不再紧张）批量规划成路线，而不必逐天精确核算：

- **解锁条件**：地图上全部 Tent（营地）**和**全部 G/g 地块（大卦/小卦）都已经被占领过，且当前查看的天数要在"这些格子全部占领完成那一天"的**第二天或以后**才能开启。
- 开启后，玩家可以在地图上自由连续画路线，**不再受当天步数/粮食是否够用的即时限制**（画的时候不结算，只是先记录路线）。
- 退出圈地模式时，系统会把这段时间画出的所有路线，从"开启圈地模式那一天"起，按每支队伍的步数上限（每天 6 步、上限 18）自动依次分配、重新结算到未来的各个天数上；如果分配完之后仍然出现步数超支或粮食不够，会提示需要减少路线或继续调整。

---

## 12. 撤销与重置

- **撤销**：只撤销当前激活队伍**最后一段**移动（不是当前查看的这一天），完整回滚路径点、粮食/积分/步数、飞雷神次数变化；但会把该队伍的 X/B/Z 增益剩余次数清零而非还原（已知的简化行为）。
- **重置**：二次确认后，所有队伍、粮食、积分、天数、飞雷神次数、G/g 占领记录全部清空，回到 Day 1 初始状态，只保留 team1 在出生点。

---

## 13. 一局游戏的资源与目标小结

把以上规则串起来，"远征"这个游戏本质上是一个**带资源约束的多智能体路径规划优化问题**：

- **目标**：在 97 天（S25）内，通过 3 支队伍的移动尽量多占领高分地块（大boss 1000 分、中boss 500 分、历练 520 分、大卦 300 分等），最大化累计积分。
- **约束**：
  - 全局共享、逐日增长的粮食池（起始 6800，每天 +1600，可结转）；
  - 每支队伍独立、每日恢复但封顶 18 的步数池；
  - 单次移动最多 18 格，且必须走连通路径（或消耗稀缺的飞雷神次数做有限的"跳跃"）。
- **可优化的机制杠杆**：
  - 尽早占领 X/B/Z 增益地块，为后续密集的占领行动省步数、省粮食、加积分；
  - 尽快集齐全部 G/g 地块，换取此后所有开销永久打 8 折；
  - 合理利用"免费探索/蹭步"在步数不够时提前用粮食"看地"，减少后续走空路；
  - 首次拿下大boss 换取额外飞雷神次数，用于突破连通性限制、抢占孤立的高分地块或传送门另一侧；
  - 利用传送门做低成本的远距离转移（代价是消耗 B/Z 剩余次数、且首次使用记入较高的"跳步"统计）；
  - 每 7 天一次的 +500 固定奖励与行动无关，属于稳定收益，不需要为了它专门调整路线。

---

## 附：与本文档相关的源码入口速查

| 规则 | 对应函数/位置（`hex_pathfinding_demo.py`） |
|---|---|
| 六边形坐标/邻接/距离 | `_center`、`_neighbors`、`_to_axial`、`_hex_dist` |
| A* 寻路 | `_astar`、`_cost` |
| 单次移动分类计费 | `_process_path_click`（约 5583~6356 行） |
| 折扣/加成公式 | `_apply_challenge_discounts`、`_apply_g_reduction`、`_apply_z_bonus`、`_get_terrain_food` |
| 免费探索/蹭步 | `_confirm_free_exploration_step`、`_confirm_settle_current_exploration`、`_is_hex_settled` |
| 飞雷神 | `_activate_fly_skill`、`_is_valid_fly_destination` |
| 传送门 | `_check_portal_teleport` |
| 增益判定 | `_activate_land_buffs`、`_derive_team_buff_state` |
| G/g 全局折扣判定 | `_are_all_g_lands_visited`、`_find_all_g_lands_complete_day` |
| 每日资源账本（权威数据源） | `_rebuild_day_records` |
| 圈地模式 | `_can_start_enclosure_mode`、`_toggle_enclosure_mode`、`_finish_enclosure_mode`、`_rebalance_enclosure_segments_from_day` |
| 撤销 | `_undo` |
| 跳步统计 | `_compute_jump_summary_before_after_g` |

地块基础数值来源：`landInfo.json`（当前 S25 版本，含 `map_files`/`season`/`team_colors` 及每个地块 token 的 `food`/`step`/`award`/`degrade`/`effective_steps`/`food_discount_rate`/`reward_bonus_rate` 等字段）。
